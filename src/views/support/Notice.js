import React from 'react'
import ContainerWrapper from '../../components/container/ContainerWrapper'

const Notice = ({ meta }) => {
  return (
    <>
      <ContainerWrapper routeMeta={meta}></ContainerWrapper>
    </>
  )
}

export default Notice
